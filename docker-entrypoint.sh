#!/bin/sh
# ============================================
# Docker 容器启动脚本
# 功能：同时启�?cron 服务和主程序
# ============================================

# 启动 cron 服务（后台运行）
# crond -f -l 2 &

# 启动主程序（前台运行�?
exec ./main